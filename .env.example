# Copy to .env (git-ignored) and fill in from your LiveKit Cloud project → Settings → Keys.
# Only the game server reads these. Never commit the real values.
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
